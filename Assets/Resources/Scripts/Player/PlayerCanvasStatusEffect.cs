using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerCanvasStatusEffect : MonoBehaviour
{
    //public static PlayerCanvasStatusEffect Instance { get; set; }
    //GameObject player;
    //PlayerCanvasStatusEffectHolder holderPrefab;

    //void Start()
    //{
    //    if (Instance != null && Instance != this) Destroy(gameObject);
    //    else Instance = this;
    //    player = GameObject.FindGameObjectWithTag("Player");
    //    holderPrefab = Resources.Load<PlayerCanvasStatusEffectHolder>("Prefabs/UI/StatusEffects/StatusEffectHolder");

    //}

    //void Update()
    //{
    //    transform.position = new Vector2(player.transform.position.x, player.transform.position.y + player.GetComponent<SpriteRenderer>().bounds.size.y / 2);
    //}

    //public void AddStatus(string name, float duration)
    //{
    //    PlayerCanvasStatusEffectHolder holder = Instantiate(holderPrefab, transform);
    //    holder.img.sprite = Resources.Load<Sprite>("Icons/StatusEffects/" + name);
    //    holder.Duration = duration;
    //}

    
}
